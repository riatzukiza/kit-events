{
    "name": "kit-events",
    "version": "0.5.1",
    "scripts": {
        "compile": "sibilant ./src/index.sibilant -o ./",

        "publish": "npm run compile;git add .;git commit -m 'new version published';git push github master;"
    },
    "dependencies": {
        "kit": "git+https://github.com/riatzukiza/kit.git",
        "kit-interface": "git+https://github.com/riatzukiza/kit-interface.git",

        "ramda": "*",
        "chokidar": "2.0.2"
    }
}
